name: CI – Trivy Scan & Terraform Plan

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write   # allows posting plan comments on PRs

env:
  AWS_REGION: eu-central-1
  TF_VERSION: "1.9.8"
  AWS_DEPLOY_ROLE_ARN: "arn:aws:iam::308488463057:role/GitHubActionsRole-platform-level"

jobs:
  # ──────────────────────────────────────────────
  # Job 1 – Trivy IaC / config scan
  # Fails the pipeline on any CRITICAL finding.
  # ──────────────────────────────────────────────
  trivy-scan:
    name: Trivy Config Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy config scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: config
          scan-ref: "."
          # Fail only on CRITICAL severity findings
          severity: CRITICAL
          exit-code: "1"
          format: table
          # Scan all IaC-relevant files in the repo
          skip-dirs: ".terraform,.git"

  # ──────────────────────────────────────────────
  # Job 2a – Terraform plan (Wave 1: Platform Foundations)
  # Runs in parallel; iam and load_balancer must complete before eks.
  # ──────────────────────────────────────────────
  terraform-plan-wave1:
    name: Terraform Plan – ${{ matrix.module }}
    runs-on: ubuntu-latest
    needs: trivy-scan

    strategy:
      fail-fast: false
      matrix:
        module:
          - iam
          - load_balancer
          - compute
          - databases
          - monitoring

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-actions-tf-plan

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init – ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: |
          terraform init -input=false \
            -backend-config="region=${{ env.AWS_REGION }}"

      - name: Terraform Validate – ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: terraform validate

      - name: Terraform Plan – ${{ matrix.module }}
        id: tf-plan
        working-directory: ${{ matrix.module }}
        run: |
          set -o pipefail
          terraform plan \
            -input=false \
            -no-color \
            -out=tfplan.binary 2>&1 | tee plan.txt
          echo "exitcode=${PIPESTATUS[0]}" >> "$GITHUB_OUTPUT"

      - name: Post plan summary to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          MODULE: ${{ matrix.module }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync(`${process.env.MODULE}/plan.txt`, 'utf8');
            const truncated = plan.length > 60000
              ? plan.substring(0, 60000) + '\n\n... (truncated)'
              : plan;
            const body = `## Terraform Plan – \`${process.env.MODULE}\`\n\`\`\`\n${truncated}\n\`\`\``;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });

      - name: Upload plan artifact – ${{ matrix.module }}
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ matrix.module }}-${{ github.run_id }}
          path: ${{ matrix.module }}/tfplan.binary
          retention-days: 7

  # ──────────────────────────────────────────────
  # Job 2b – Terraform plan (Wave 3: logging)
  # Independent plan; apply will run after eks apply.
  # ──────────────────────────────────────────────
  terraform-plan-wave3:
    name: Terraform Plan – ${{ matrix.module }}
    runs-on: ubuntu-latest
    needs: trivy-scan

    strategy:
      fail-fast: false
      matrix:
        module:
          - logging

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-actions-tf-plan

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init – ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: |
          terraform init -input=false \
            -backend-config="region=${{ env.AWS_REGION }}"

      - name: Terraform Validate – ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: terraform validate

      - name: Terraform Plan – ${{ matrix.module }}
        id: tf-plan
        working-directory: ${{ matrix.module }}
        run: |
          set -o pipefail
          terraform plan \
            -input=false \
            -no-color \
            -out=tfplan.binary 2>&1 | tee plan.txt
          echo "exitcode=${PIPESTATUS[0]}" >> "$GITHUB_OUTPUT"

      - name: Post plan summary to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          MODULE: ${{ matrix.module }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync(`${process.env.MODULE}/plan.txt`, 'utf8');
            const truncated = plan.length > 60000
              ? plan.substring(0, 60000) + '\n\n... (truncated)'
              : plan;
            const body = `## Terraform Plan – \`${process.env.MODULE}\`\n\`\`\`\n${truncated}\n\`\`\``;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });

      - name: Upload plan artifact – ${{ matrix.module }}
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ matrix.module }}-${{ github.run_id }}
          path: ${{ matrix.module }}/tfplan.binary
          retention-days: 7

  # ──────────────────────────────────────────────
  # Job 3a – Terraform apply (Wave 1: Platform Foundations)
  # Runs first; eks depends on iam and load_balancer SSM outputs.
  # ──────────────────────────────────────────────
  terraform-apply-wave1:
    name: Terraform Apply – ${{ matrix.module }}
    runs-on: ubuntu-latest
    needs: terraform-plan-wave1
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    strategy:
      fail-fast: true    # eks must not start if wave1 fails
      matrix:
        module:
          - iam
          - load_balancer
          - compute
          - databases
          - monitoring

    environment: production

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-actions-tf-apply

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init – ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: |
          terraform init -input=false \
            -backend-config="region=${{ env.AWS_REGION }}"

      - name: Download plan artifact – ${{ matrix.module }}
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ matrix.module }}-${{ github.run_id }}
          path: ${{ matrix.module }}

      - name: Terraform Apply – ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: terraform apply -input=false -auto-approve tfplan.binary

  # ──────────────────────────────────────────────
  # Job 3b – Terraform plan (Wave 2: eks with resolved vars)
  # Runs after Wave 1 apply on push to main. Reads resolved
  # alb_sg_id and eks_admins_arn from SSM after iam + load_balancer apply.
  # Produces tfplan.binary and plan summary for review before manual apply.
  # ──────────────────────────────────────────────
  terraform-plan-eks-resolved:
    name: Terraform Plan – eks (Resolved Vars)
    runs-on: ubuntu-latest
    needs: terraform-apply-wave1
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-actions-tf-plan

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Read cross-module SSM outputs
        id: ssm
        run: |
          echo "alb_sg_id=$(aws ssm get-parameter --name /platform/alb/security_group_id --query Parameter.Value --output text)" >> "$GITHUB_OUTPUT"
          echo "eks_admins_arn=$(aws ssm get-parameter --name /platform/iam/eks_admins_arn --query Parameter.Value --output text)" >> "$GITHUB_OUTPUT"

      - name: Terraform Init – eks
        working-directory: eks
        run: |
          terraform init -input=false \
            -backend-config="region=${{ env.AWS_REGION }}"

      - name: Terraform Plan – eks (with resolved vars)
        id: tf-plan
        working-directory: eks
        run: |
          set -o pipefail
          terraform plan \
            -input=false \
            -no-color \
            -var="alb_sg_id=${{ steps.ssm.outputs.alb_sg_id }}" \
            -var="eks_admins_arn=${{ steps.ssm.outputs.eks_admins_arn }}" \
            -out=tfplan.binary 2>&1 | tee plan.txt
          echo "exitcode=${PIPESTATUS[0]}" >> "$GITHUB_OUTPUT"

      - name: Write plan summary to step summary
        run: |
          echo "## Terraform Plan – \`eks\` (Resolved Vars)" >> "$GITHUB_STEP_SUMMARY"
          echo "\`\`\`" >> "$GITHUB_STEP_SUMMARY"
          head -c 60000 eks/plan.txt >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "\`\`\`" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload plan artifact – eks (resolved)
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-eks-resolved-${{ github.run_id }}
          path: |
            eks/tfplan.binary
            eks/plan.txt
          retention-days: 2

  # ──────────────────────────────────────────────
  # Job 3c – Terraform apply (Wave 2: eks)
  # Requires manual review/approval in the production environment
  # after reviewing the plan produced by terraform-plan-eks-resolved.
  # ──────────────────────────────────────────────
  terraform-apply-eks:
    name: Terraform Apply – eks
    runs-on: ubuntu-latest
    needs: terraform-plan-eks-resolved
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    environment: production

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-actions-tf-apply

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init – eks
        working-directory: eks
        run: |
          terraform init -input=false \
            -backend-config="region=${{ env.AWS_REGION }}"

      - name: Download plan artifact – eks
        uses: actions/download-artifact@v4
        with:
          name: tfplan-eks-resolved-${{ github.run_id }}
          path: eks

      - name: Terraform Apply – eks
        working-directory: eks
        run: terraform apply -input=false -auto-approve tfplan.binary

  # ──────────────────────────────────────────────
  # Job 3c – Terraform apply (Wave 3: logging)
  # Runs after eks apply so that eks-node-role and eks-cluster-role exist.
  # Runs regardless of eks apply outcome as long as logging plan succeeded.
  # No environment: production specified to avoid requiring an extra approval.
  # ──────────────────────────────────────────────
  terraform-apply-wave3:
    name: Terraform Apply – ${{ matrix.module }}
    runs-on: ubuntu-latest
    needs:
      - terraform-plan-wave3
      - terraform-apply-eks
    if: |
      always() &&
      needs.terraform-plan-wave3.result == 'success' &&
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main'

    strategy:
      fail-fast: false
      matrix:
        module:
          - logging

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-actions-tf-apply

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init – ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: |
          terraform init -input=false \
            -backend-config="region=${{ env.AWS_REGION }}"

      - name: Download plan artifact – ${{ matrix.module }}
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ matrix.module }}-${{ github.run_id }}
          path: ${{ matrix.module }}

      - name: Terraform Apply – ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: terraform apply -input=false -auto-approve tfplan.binary
